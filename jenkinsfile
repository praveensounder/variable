pipeline {
    agent any

    stages {
       stage('Terraform Plan') {
           steps {
    bat 'terraform init'
    bat 'terraform plan'
  }
       }

  stage ('Terraform Apply') {
      steps {
    bat 'terraform apply -auto-approve'
  }
  }
   stage ('success'){
            steps {
                script {
                    currentBuild.result = 'SUCCESS'
                }
            }
        }
        }
  post {
    failure {
         
        emailext to: 'praveensounder98@gmail.com',
             attachLog: true,
             subject: "Failed Pipeline: ${currentBuild.fullDisplayName}",
             body: "ec2 creation fail ${env.BUILD_URL}"
    }
    }
}
